Fields that come from SCF:
['F','Eelec','rho','eorb','occ_rho_mask', 'entropy', 'fermi_energy']
['dQ','dQinit','dQcurr']

Fields that are indexed by basis size:
Erep (X)
rho (X)
S (X)
phiS (X)
Sevals (X)
G (X)
dQ (X)
Eelec (X)
eorb (X)
glabels
gather_for_rep
segsum_for_rep
occ_rho_mask (X)
occ_eorb_mask (X)
qneutral (X)
atom_ids
norbs_atom
zcounts (X)
names (X)
iconfigs (X)
Etot (X)

Additional information to be saved per molecule:
geoms
iconfig
name (obviously)


Anything indexed by basis_sizes can be searched in order by glabels. 

Dataset structure:
Molec
    iconfig
        Zs : data
        Coords : data
        Erep : data
        rho : data
        etc. : data
        All the fields and their associated information
    
    iconfig 
        All the fields and stuff

Dataset2 (TODO:)
Batch num
    things for that batch

Thus, the iconfigs are used to index the molecules
Keys to save for the batches themselves:
'geoms', (IGNORE, RECONSTITUTE LATER) 
'mod_raw', 
'rot_tensors', 
'models', 
'onames', 
'basis_sizes', 
'glabels', 
'gather_for_rot', 
'gather_for_oper', 
'gather_for_rep', 
'segsum_for_rep', 
'atom_ids', 
'norbs_atom', 
'names', 
'iconfigs'

Assumption is wrong: The most time-consuming step is the create_batch step